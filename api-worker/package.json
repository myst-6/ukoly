{
  "name": "api-worker",
  "version": "1.0.0",
  "description": "API worker for code execution platform",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.1.3"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "wrangler": "^4.26.0"
  }
}
